---
title: CLI
description: 'Single bin, subcommand dispatch, env var ↔ flag pairing.'
---

The CLI is `{{name}}` — a single bin that dispatches to subcommands. Every tool exposed via MCP is automatically also a subcommand here.

## Subcommands

```
{{name}} mcp [--http] [--port N] [--bind H]   run the MCP server
{{name}} tui                                   launch the Ink TUI
{{name}} doctor                                preflight checks
{{name}} repl                                  interactive REPL
{{name}} health                                one-shot health snapshot
{{name}} noop --input <text> [--upper]         demo tool invocation
```

## Global flags

| Flag | Description |
|------|-------------|
| `--json` | Machine-readable JSON output. |
| `-q, --quiet` | Suppress non-error output. |
| `-v, --verbose` | Debug-level info to stderr. |
| `--no-color` | Disable colors (also auto-disabled when stdout is not a TTY). |

## Env vars ↔ flags

Every recognized env var is also accepted as a CLI flag (see `@george43g/cli-kit/env-flag-binder`). Examples:

| Env | Flag | Purpose |
|-----|------|---------|
| `MCP_HTTP_TOKEN` | `--http-token` | Bearer token for HTTP transport |
| `MCP_LOG_DIR` | `--log-dir` | Override default `$TMPDIR/{{name}}/` |
| `MCP_DISABLE_NATIVE` | `--no-native` | Force TS fallback (skip Rust accel) |
| `MCP_TOOL_TIMEOUT_DEFAULT_MS` | `--tool-timeout-ms` | Per-tool default timeout |
| `{{NAME_UPPER}}_DEV` | `--dev` | Enable dev-only tools (`get_logs`) |
